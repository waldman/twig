modules_path: github.com/waldman/twig-infra-example//modules
modules_ref:  v1.0.0

backend:
  bucket:         my-terraform-state
  region:         us-east-1
  dynamodb_table: my-terraform-locks  # optional state locking
  profile:        myprofile
